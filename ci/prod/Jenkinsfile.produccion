pipeline {
    agent any
    stages {
        stage('Clonar Repositorio') {
            steps {
                git credentialsId: 'token_git',
                    url: 'https://github.com/Marjorie-1004/final-react-2025.git',
                    branch: 'main'
            }
        }
        stage('test') {
            steps {
                sh '''
                  ls; gcloud version
                '''
            }
        }
    }
}
